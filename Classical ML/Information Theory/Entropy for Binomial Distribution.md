# Entropy for Binomial Distribution

Let $Y \sim Binomial$ such that $P(y = C_{0}) = P$ and $P(y = C_{1}) = 1 - p$

Then, the [[Entropy]] can be defined as
$H(Y).H(D) = p.\log\left( \frac{1}{p} \right) + (1-p) \log \frac{1}{1 - p}$
